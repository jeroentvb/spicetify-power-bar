const CHANGE_NOTES = `
* Fixed the powerbar activation keys not working on MacOS. Default activation keys are now "option + k" on MacOS and "ctrl + space" on Windows/Linux. You can change this in the settings.
`;

export default CHANGE_NOTES;
